<Page
    x:Class="LichessXbox.Views.GamesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:LichessXbox.Controls"
    Background="Transparent">

    <Grid Padding="{StaticResource PagePadding}">

        <!-- Signed out -->
        <StackPanel x:Name="SignInPrompt" Visibility="Collapsed"
                    VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="18" MaxWidth="520">
            <TextBlock Text="Sign in to see your games" FontSize="{StaticResource TitleSize}" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" TextAlignment="Center" />
            <TextBlock Text="Your game history, ratings, and friends live on your Lichess account."
                       FontSize="{StaticResource BodySize}" Foreground="{StaticResource TextSecondaryBrush}" TextAlignment="Center" TextWrapping="Wrap" />
            <Button x:Name="GoSignInButton" Style="{StaticResource PrimaryButton}" Click="GoSignIn_Click" HorizontalAlignment="Center" Content="Sign in with Lichess" />
        </StackPanel>

        <!-- The board grid scrolls itself (it must NOT be nested in a ScrollViewer, or its
             ItemsWrapGrid is measured with infinite height and realises every board at once). -->
        <Grid x:Name="GamesContent" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>

            <!-- Games (grid of board thumbnails) -->
            <Grid Grid.Column="0" Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- In progress: live games you can resume (shown only when you have some). The
                     home for in-progress games, so they're not hidden behind a floating overlay. -->
                <StackPanel Grid.Row="0" x:Name="InProgressSection" Spacing="10" Margin="0,0,0,20" Visibility="Collapsed">
                    <TextBlock Text="In progress" FontSize="{StaticResource BodySize}" FontWeight="SemiBold" Foreground="{StaticResource AccentGreenLightBrush}" />
                    <ListView x:Name="InProgressList" SelectionMode="None" IsItemClickEnabled="True" ItemClick="Resume_ItemClick"
                              ScrollViewer.HorizontalScrollMode="Enabled" ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                              ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsStackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border CornerRadius="14" Padding="10" Width="248" Margin="0,0,4,0"
                                        Background="{StaticResource AppSurfaceHighBrush}" BorderBrush="{StaticResource HairlineBrush}" BorderThickness="1">
                                    <Grid ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Width="76" Height="76" CornerRadius="8">
                                            <controls:ChessBoardControl Fen="{Binding Fen}" WhiteAtBottom="{Binding WhiteAtBottom}"
                                                                        Interactive="False" IsTabStop="False" />
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <Ellipse Width="10" Height="10" VerticalAlignment="Center" Fill="{StaticResource AccentGreenLightBrush}"
                                                         Opacity="{Binding IsMyTurn, Converter={StaticResource BoolToOpacity}}" />
                                                <TextBlock Text="{Binding OpponentName}" FontSize="{StaticResource BodySize}" FontWeight="SemiBold"
                                                           Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                            <TextBlock Text="{Binding TypeText}" FontSize="{StaticResource CaptionSize}" Foreground="{StaticResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding TurnText}" FontSize="{StaticResource CaptionSize}" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <TextBlock Grid.Row="1" Text="Your games" FontSize="{StaticResource TitleSize}" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,14" />
                <StackPanel Grid.Row="2" Spacing="12" Margin="0,0,0,10">
                    <TextBlock x:Name="NoGames" Text="No games yet." FontSize="{StaticResource CaptionSize}" Foreground="{StaticResource TextSecondaryBrush}" Visibility="Collapsed" />
                    <Button x:Name="RetryGamesButton" Style="{StaticResource TileButton}" Click="Retry_Click"
                            Content="Refresh" HorizontalAlignment="Left" HorizontalContentAlignment="Center" MinWidth="160" Visibility="Collapsed" />
                    <ProgressRing x:Name="Busy" IsActive="False" Visibility="Collapsed" Width="34" Height="34" HorizontalAlignment="Left"
                                  Foreground="{StaticResource AccentGreenLightBrush}" />
                </StackPanel>
                <GridView Grid.Row="3" x:Name="GamesList" SelectionMode="None" IsItemClickEnabled="True" ItemClick="Game_ItemClick"
                          Padding="0,0,0,16" ScrollViewer.HorizontalScrollMode="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="14" Padding="10" Width="184"
                                    Background="{StaticResource AppSurfaceHighBrush}" BorderBrush="{StaticResource HairlineBrush}" BorderThickness="1">
                                <StackPanel Spacing="8">
                                    <Border CornerRadius="8" Width="164" Height="164">
                                        <controls:ChessBoardControl Fen="{Binding FinalFen}" WhiteAtBottom="{Binding PlayerWhite}"
                                                                    Interactive="False" IsTabStop="False" />
                                    </Border>
                                    <TextBlock Text="{Binding Headline}" FontSize="{StaticResource CaptionSize}" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding ResultText}" FontSize="{StaticResource CaptionSize}"
                                               Foreground="{Binding Outcome, Converter={StaticResource OutcomeBrush}}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </Grid>

            <!-- Rating + friends -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled">
                <StackPanel Spacing="16">
                    <Border Style="{StaticResource CardBorder}" Padding="20">
                        <StackPanel Spacing="10">
                            <TextBlock x:Name="RatingTitle" Text="Blitz rating" FontSize="{StaticResource BodySize}" FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentGreenLightBrush}" />
                            <Border Height="170" CornerRadius="10" Background="{StaticResource SurfaceSunkenBrush}">
                                <Canvas x:Name="GraphCanvas" />
                            </Border>
                            <TextBlock x:Name="RatingRange" Text="" FontSize="{StaticResource CaptionSize}" Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardBorder}" Padding="20">
                        <StackPanel Spacing="10">
                            <TextBlock Text="Following" FontSize="{StaticResource BodySize}" FontWeight="SemiBold" Foreground="{StaticResource AccentGreenLightBrush}" />
                            <TextBlock x:Name="NoFollowing" Text="Not following anyone yet." FontSize="{StaticResource CaptionSize}"
                                       Foreground="{StaticResource TextSecondaryBrush}" Visibility="Collapsed" />
                            <!-- ListView (not ItemsControl) so its rows are gamepad focus stops — that's
                                 what makes the right column reachable/scrollable at 10 feet. Inner scroll
                                 disabled so the outer card ScrollViewer does the scrolling. -->
                            <ListView x:Name="FollowingList" SelectionMode="None" IsItemClickEnabled="False"
                                      ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" FontSize="{StaticResource CaptionSize}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,2" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
